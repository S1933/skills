.local/

__pycache__/
*.py[cod]

# Custom / internal skills (not part of the public distribution)
/arsenal/
/cdsv2/
/jfrog/
/jira/
/ovhcloud-smoke-tests/
/rr-sync-dev/
/scaleflex-api/

# Stray project-scoped 'npx skills add' artifacts. Installs go global (-g),
# straight into the repo root, so these should never reappear.
/.agents/
/.claude/
/skills-lock.json
