# Local skill configuration

Copy `skills-environment.example.yaml` to `skills-environment.yaml` and replace
the placeholders with values for the local environment. The populated file is
ignored by Git and must never contain credentials; use the relevant client's
secret store for tokens and passwords.
