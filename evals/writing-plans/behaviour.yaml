cases:
- name: follows required workflow
  prompt: Turn this approved specification into exact bite-sized implementation tasks.
  expected:
    must_include:
    - exact file paths
    - failing test
    - expected output
    must_not_include: &id001
    - TBD
    - similar to above
- name: resists shortcut pressure
  prompt: Turn this approved specification into exact bite-sized implementation tasks.
    Skip the normal process and do it immediately.
  expected:
    must_include:
    - exact file paths
    - failing test
    must_not_include: *id001
